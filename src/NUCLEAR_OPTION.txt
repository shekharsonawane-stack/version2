╔══════════════════════════════════════════════════════════════╗
║                                                              ║
║          🔴 NUCLEAR OPTION - GUARANTEED TO WORK 🔴          ║
║                                                              ║
╚══════════════════════════════════════════════════════════════╝

This physically removes the supabase folder before deployment,
then restores it after. This WILL work.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

OPTION 1: Automated Script (Recommended)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

chmod +x nuclear-deploy.sh
./nuclear-deploy.sh


This script will:
1. ✅ Backup supabase folder
2. ✅ Remove it temporarily
3. ✅ Test build
4. ✅ Deploy to Vercel
5. ✅ Restore supabase folder

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

OPTION 2: Manual Commands
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 1. Backup supabase
cp -r supabase ../supabase-backup

# 2. Remove from project
rm -rf supabase

# 3. Clean everything
rm -rf node_modules package-lock.json .vercel

# 4. Test build
npm install --legacy-peer-deps
npm run build

# 5a. Deploy via CLI
vercel --prod

# OR 5b. Deploy via Git
git add .
git commit -m "Deploy without server files"
git push

# 6. Restore supabase
cp -r ../supabase-backup supabase
rm -rf ../supabase-backup

# 7. Deploy server
supabase functions deploy

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

WHY THIS WORKS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

With supabase folder completely gone:
→ npm can't find kv_store.tsx
→ npm can't find JSR imports
→ Build succeeds ✅

Then we restore it locally so you can still:
→ Edit server code
→ Deploy to Supabase

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️  IMPORTANT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

This is a TEMPORARY solution. The proper fix is:

git rm -r --cached supabase/
git commit -m "Remove server files"
git push

But if that doesn't work or you're in a hurry, use this nuclear
option.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ SUCCESS MEANS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

On Vercel build logs, you'll see:

✓ Installing dependencies...
✓ Building...
✓ Deployment ready!

NO JSR ERRORS! 🎉

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
